#include <bits/stdc++.h>

using namespace std;

const int N = 1e4 + 5;
int n;
bool a[N];
int b[N];

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  cin >> k;
  for (int i = 1; i <= k; i++) {
    cin >> b[k];
  }
  

  return 0;
}
