#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 5;

int n;
int a[N], dif[N];

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  for (int i = 1; i < n; i++) {
    
  }

  return 0;
}
