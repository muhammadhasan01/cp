#include <bits/stdc++.h>

using namespace std;

const int M = 1e9 + 7;
int n, q, k;
int a[N];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> k >> q;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    while (q--) {
        char x;
        cin >> x;
        if (x == 'Q') {
            
        } else if (x == 'U') {

        }
    }

    return 0;
}
