#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int N = 1e2 + 5;
int n;
ll h;
pair<ll, ll> p[N][15];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> h;
    for (int i = 1; i <= n; i++) {
        cin >> p[i][1].first >> p[i][1].second;
    }
    for (int i = 1; i <= 100 * 100; i++) {
        
    }

    return 0;
}
