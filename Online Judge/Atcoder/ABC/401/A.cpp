#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int s;
    cin >> s;
    cout << (200 <= s && s <= 299 ? "Success" : "Failure") << '\n';
    
    return 0;
}