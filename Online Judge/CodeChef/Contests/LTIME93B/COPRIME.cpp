#include <bits/stdc++.h>

using namespace std;

const int ans = 1e9 + 7;

void solve() {
    int L, R;
    cin >> L >> R;
    cout << ans << '\n';
}

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int tc;
    cin >> tc;
    for (int T = 1; T <= tc; T++) {
        solve();
    }

    return 0;
}