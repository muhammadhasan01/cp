#include <bits/stdc++.h>

using namespace std;

const int N = 5e5 + 5;

int n;
int a[N], b[N];

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a[i] >> b[i];
    }
    

    return 0;
}