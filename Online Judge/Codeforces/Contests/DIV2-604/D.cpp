#include <bits/stdc++.h>

using namespace std;

int n;
int a, b, c, d;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> a >> b >> c >> d;
    n = a + b + c + d;
    

    return 0;
}
