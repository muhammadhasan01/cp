#include <bits/stdc++.h>

using namespace std;

string s, t;
int n, m;

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> s >> t;
    n = s.length();
    m = t.length();
    

    return 0;
}