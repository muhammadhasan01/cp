#include <bits/stdc++.h>

using namespace std;

int tc;
long long x;

int main() { 
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  cin >> tc;
  while (tc--) {
    cin >> x;
    cout << 1 << " " << x - 1 << '\n';
  }

  return 0;
}