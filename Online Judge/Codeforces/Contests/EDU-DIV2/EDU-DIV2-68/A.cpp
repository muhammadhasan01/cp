#include<bits/stdc++.h>
using namespace std;

int tc;
long long n,x;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> tc;
    while(tc--){
        cin >> n >> x;
        cout << 2*x << '\n';
    }

    return 0;
}
