#include <bits/stdc++.h>

using namespace std;

const int N = 3e5 + 5;
const int M = 1e6 + 5;

int n, w;
int dp[N];
set<int> s[M];
int point[N];

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  int maks = 0;
  cin >> n >> w;
  for (int i = 1; i <= w; i++) {
    int k;
    cin >> k;
    for (int j = 1; j <= k; j++) {
      int x;
      cin >> x;
    }

  }

  return 0;
}
