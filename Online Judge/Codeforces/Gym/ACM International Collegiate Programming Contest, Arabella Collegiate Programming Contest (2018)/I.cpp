#include <bits/stdc++.h>

using namespace std;

const long long M = 1e9 + 7;

int tc;
long long n, m;

void solve() {
    cin >> n >> m;
    
}

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> tc;
    while (tc--) {
        solve();
    }

    return 0;
}