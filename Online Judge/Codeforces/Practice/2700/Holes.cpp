#include <bits/stdc++.h>

using namespace std;

int n, m;
int a[N];

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    for (int i = 1; i <= m; i++) {
        int tp, u;
        cin >> tp >> u;
        if (tp == 0) {
            int v;
            cin >> v;
        } else {
            
        }
    }

    return 0;
}