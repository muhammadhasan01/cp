#include<bits/stdc++.h>
using namespace std;

#define mp make_pair
#define pb push_back
#define inf int_MAX
#define MOD 1000000007
#define MEM(a,b) memset(a,(b),sizeof(a))

typedef long long ll;
typedef pair<int,int> pii;
typedef pair<long long, long long> pll;

// Divide into two even numbers

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n; cin >> n; if(n%2==0 && n >= 4){cout<<"YES\n";}else{cout<<"NO\n";}
    return 0;
}
