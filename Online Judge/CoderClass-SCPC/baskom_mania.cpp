#include<bits/stdc++.h>
using namespace std;

const int N = 1e5+5;
int tc;
int n;
long long m,k;
long long a[N], pre[N];

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> tc;
    while(tc--){
        cin >> n >> m >> k;
        for(int i=1;i<=n;i++){
            cin >> a[i];
        }
    }


    return 0;
}
