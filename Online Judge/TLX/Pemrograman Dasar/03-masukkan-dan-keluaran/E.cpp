#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    
    double a, t;
    cin >> a >> t;
    cout << fixed << setprecision(2) << a * t / 2 << '\n';
    
    return 0;
}