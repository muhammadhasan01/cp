#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    
    int x;
    int sum = 0;
    while (cin >> x) {
        sum += x;
    }
    cout << sum << endl;
    
    return 0;
}