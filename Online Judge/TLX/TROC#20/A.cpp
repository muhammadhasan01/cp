#include <bits/stdc++.h>

using namespace std;

int x, y;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> x >> y;
    cout << (x >= y ? "YES" : "NO") << '\n';

    return 0;
}